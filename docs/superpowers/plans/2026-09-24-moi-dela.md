# «Мои дела» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Список всего, что ждёт действия жителя (заявки, брони, отказы модерации, просроченный возврат, закупки, очередь модерации), — блоком на главной, пунктом меню с числом и своей страницей `/poselenie/dela`.

**Architecture:** Сервис `MyTasks` собирает дела на лету из текущих статусов — новых таблиц нет. Источники — в основном **уже существующие выборки** репозиториев (`listIncoming`, `listByBorrower`, `listByFamily`, `listByOrganizer`, `listByParticipant`, `listPending`); добавляются лишь два столбца: `unpaid_count` в итоги закупок и `trip_status` в входящие брони. Результат запоминается на время запроса и используется главной, меню и страницей.

**Tech Stack:** PHP 8.3 без фреймворка (`residents/`), шаблоны на чистом PHP через `View`, PHPUnit 11 на SQLite in-memory. Локально PHP нет — тесты гоняются на сервере `abconsult`.

**Спецификация:** `docs/superpowers/specs/2026-09-24-moi-dela-design.md`.

**Уточнение к спеке при планировании:** спека предлагала новые методы `requestsForOwner()`, `overdueForBorrower()`, `rejectedByFamily()`, `countPending()` и т. п. Оказалось, что почти все нужные выборки в репозиториях уже есть, — план переиспользует их и фильтрует в сервисе. Поведение то же, нового SQL почти нет.

---

## Как гонять тесты

Все команды — из Git Bash. Прогон одного тестового класса (подставьте имя в `--filter`):

```bash
cd /c/Projects/skaz-kray-astro/residents && tar -czf - --exclude=./vendor --exclude=./.phpunit.cache --exclude=./public/uploads --exclude=./.git --exclude=./var . 2>/dev/null | ssh abconsult 'tar -xzf - -C /root/ledger-test && cd /root/ledger-test && php8.3 vendor/bin/phpunit --filter MyTasksTest'
```

Весь набор — та же команда без `--filter MyTasksTest`.

Коммиты — только через heredoc (`git commit -F - <<'MSG' … MSG`): синтаксис PowerShell `@'…'@` в Git Bash тихо портит сообщение. Заголовок без префикса `@ `. Последняя строка сообщения:

```
Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
```

## Файлы

| Файл | Что |
|---|---|
| `residents/src/Repository/PurchaseRepository.php` | в `TOTALS` — `unpaid_count` |
| `residents/src/Repository/TripBookingRepository.php` | в `listIncoming()` — `t.status AS trip_status` |
| `residents/src/Service/MyTasks.php` | **новый** — сборщик дел |
| `residents/src/Controller/TasksController.php` | **новый** — страница «Мои дела» |
| `residents/src/templates/tasks/index.php` | **новый** — разметка страницы |
| `residents/public/index.php` | маршрут `GET /poselenie/dela` |
| `residents/src/Controller/AppController.php` | передать дела на главную |
| `residents/src/templates/app/home.php` | блок «Ждут вас: N» |
| `residents/src/templates/layout.php` | пункт меню «Мои дела» с числом |
| `residents/public/assets/residents.css` | стили блока, списка и числа в меню |
| `residents/tests/MyTasksTest.php` | **новый** — все виды дел |
| `residents/tests/TasksPageTest.php` | **новый** — рендер страницы |
| `residents/tests/PurchaseRepositoryTest.php`, `TripBookingRepositoryTest.php`, `AppHomeTilesTest.php`, `LayoutMenuTest.php` | дополнить |

---

### Task 1: Число неоплаченных заказов в итогах закупки

**Files:**
- Modify: `residents/src/Repository/PurchaseRepository.php:28-31` (константа `TOTALS`)
- Test: `residents/tests/PurchaseRepositoryTest.php`

- [ ] **Step 1: Write the failing test** — добавить в конец класса `PurchaseRepositoryTest`, перед закрывающей `}`:

```php
    public function test_totals_count_orders_without_payment(): void
    {
        // «Мои дела» просят организатора отметить оплату, пока есть неоплаченные.
        $id = $this->newPurchase();
        $paid = $this->orders->place($id, $this->organizer, '2', null, self::NOW);
        $this->orders->place($id, $this->neighbour, '3', null, self::NOW);
        $this->orders->setPaid($paid, true, self::NOW);

        $p = $this->purchases->listByOrganizer($this->organizer)[0];
        $this->assertSame(1, (int) $p['unpaid_count']);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: команда из «Как гонять тесты» с `--filter PurchaseRepositoryTest`
Expected: FAIL — `Undefined array key "unpaid_count"`.

- [ ] **Step 3: Write minimal implementation** — в `PurchaseRepository` заменить константу `TOTALS` целиком:

```php
    private const TOTALS = '
        (SELECT COALESCE(SUM(o.qty), 0) FROM purchase_orders o WHERE o.purchase_id = p.id) AS collected_qty,
        (SELECT COUNT(*) FROM purchase_orders o WHERE o.purchase_id = p.id) AS participants,
        (SELECT COALESCE(SUM(o.qty), 0) FROM purchase_orders o WHERE o.purchase_id = p.id AND o.paid_at IS NOT NULL) AS paid_qty,
        (SELECT COUNT(*) FROM purchase_orders o WHERE o.purchase_id = p.id AND o.paid_at IS NULL) AS unpaid_count';
```

- [ ] **Step 4: Run test to verify it passes**

Run: та же команда с `--filter PurchaseRepositoryTest`
Expected: PASS, все тесты класса зелёные.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Repository/PurchaseRepository.php residents/tests/PurchaseRepositoryTest.php && git commit -F - <<'MSG'
Закупки: число неоплаченных заказов в итогах

Нужно «Моим делам»: организатору закупки в стадии «привезли» напоминаем
отметить оплату, пока есть заказы без отметки.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 2: Статус поездки во входящих бронях

**Files:**
- Modify: `residents/src/Repository/TripBookingRepository.php` (метод `listIncoming`, первая строка SQL)
- Test: `residents/tests/TripBookingRepositoryTest.php`

При отмене поездки (`TripController::cancelTrip`) её брони остаются `requested` — без статуса поездки «Мои дела» просили бы водителя ответить на бронь в отменённой поездке.

- [ ] **Step 1: Write the failing test** — добавить в конец класса `TripBookingRepositoryTest`:

```php
    public function test_incoming_carries_trip_status(): void
    {
        // Отмена поездки брони не трогает — отличить их можно только по статусу поездки.
        $this->bookings->create($this->tripId, $this->passenger, 1, null, '2026-08-29 10:00:00');
        $this->trips->setStatus($this->tripId, 'cancelled');

        $row = $this->bookings->listIncoming($this->driver, ['requested'])[0];
        $this->assertSame('cancelled', $row['trip_status']);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `--filter TripBookingRepositoryTest`
Expected: FAIL — `Undefined array key "trip_status"`.

- [ ] **Step 3: Write minimal implementation** — в `listIncoming()` заменить первую строку SQL

```php
        $sql = 'SELECT b.*, t.origin, t.destination, t.trip_date, t.trip_time, p.name AS passenger_name
```

на

```php
        $sql = 'SELECT b.*, t.origin, t.destination, t.trip_date, t.trip_time, t.status AS trip_status, p.name AS passenger_name
```

- [ ] **Step 4: Run test to verify it passes**

Run: `--filter TripBookingRepositoryTest`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Repository/TripBookingRepository.php residents/tests/TripBookingRepositoryTest.php && git commit -F - <<'MSG'
Поездки: статус поездки во входящих бронях

При отмене поездки её брони остаются «ждёт подтверждения». «Моим делам»
нужно их отличать, чтобы не просить водителя ответить на бронь в отменённой
поездке.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 3: Сборщик `MyTasks` — каркас и заявки на инструменты

Каркас (форма дела, сортировка, выключатели разделов, изоляция сбоев, запоминание на запрос) и первый источник. Остальные источники добавляются в Task 4–8.

**Files:**
- Create: `residents/src/Service/MyTasks.php`
- Create: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing test** — создать `residents/tests/MyTasksTest.php`:

```php
<?php
declare(strict_types=1);
namespace SkazResidents\Tests;
use PHPUnit\Framework\TestCase;
use SkazResidents\{Database, Sections};
use SkazResidents\Service\MyTasks;
use SkazResidents\Repository\{
    FamilyRepository, ToolRepository, ToolLoanRepository, BookRepository, BookLoanRepository,
    TripRepository, TripBookingRepository, ProductRepository, DiaryRepository,
    PurchaseRepository, PurchaseOrderRepository, SectionSettingsRepository
};

/**
 * «Мои дела»: каждое дело появляется при своём условии и исчезает после
 * действия — это и есть весь контракт списка, собранного из текущих статусов.
 */
final class MyTasksTest extends TestCase
{
    private const TODAY = '2026-09-24';
    private const NOW = '2026-09-20 10:00:00';
    private int $me;
    private int $neighbour;

    protected function setUp(): void
    {
        $_SESSION = [];
        make_test_db();
        Sections::reset();
        MyTasks::reset();
        $fam = new FamilyRepository();
        $this->me = $fam->createPending('me@skaz-kray.ru', 'H', 'Семья Шубиных');
        $this->neighbour = $fam->createPending('nei@skaz-kray.ru', 'H', 'Семья Руденко');
    }

    /** @return array<int,array<string,mixed>> */
    private function tasks(bool $editor = false, string $today = self::TODAY, ?int $who = null): array
    {
        return (new MyTasks())->build($who ?? $this->me, $editor, $today);
    }

    /** @return array<int,string> */
    private function kinds(bool $editor = false, string $today = self::TODAY, ?int $who = null): array
    {
        return array_column($this->tasks($editor, $today, $who), 'kind');
    }

    public function test_request_for_my_tool_waits_until_decided(): void
    {
        $tool = (new ToolRepository())->create($this->me, 'Дрель', 'Электро', null, null, null, self::NOW);
        $loans = new ToolLoanRepository();
        $loan = $loans->create($tool, $this->neighbour, null, '2026-10-01', self::NOW);

        $t = $this->tasks();
        $this->assertSame(['tool_request'], array_column($t, 'kind'));
        $this->assertSame('Заявка на «Дрель»', $t[0]['title']);
        $this->assertSame('Семья Руденко · до 1 октября 2026', $t[0]['detail']);
        $this->assertSame('/poselenie/instrumenty/moi', $t[0]['link']);
        $this->assertFalse($t[0]['urgent']);

        $loans->give($loan, self::NOW);
        $this->assertSame([], $this->kinds());
    }

    public function test_nothing_to_do_is_an_empty_list(): void
    {
        $this->assertSame([], $this->tasks());
    }

    public function test_disabled_section_is_silent(): void
    {
        $tool = (new ToolRepository())->create($this->me, 'Дрель', 'Электро', null, null, null, self::NOW);
        (new ToolLoanRepository())->create($tool, $this->neighbour, null, null, self::NOW);
        (new SectionSettingsRepository())->setEnabled('instrumenty', false, self::NOW);
        Sections::reset();

        $this->assertSame([], $this->kinds());
    }

    public function test_for_current_is_empty_for_guest_and_computed_once_per_request(): void
    {
        $this->assertSame([], MyTasks::forCurrent());

        $_SESSION['family_id'] = $this->me;
        $tools = new ToolRepository();
        $loans = new ToolLoanRepository();
        $loans->create($tools->create($this->me, 'Дрель', 'x', null, null, null, self::NOW), $this->neighbour, null, null, self::NOW);
        $this->assertCount(1, MyTasks::forCurrent());

        // Запомнено на запрос: главная, блок и меню берут один и тот же список.
        $loans->create($tools->create($this->me, 'Пила', 'x', null, null, null, self::NOW), $this->neighbour, null, null, self::NOW);
        $this->assertCount(1, MyTasks::forCurrent());
        MyTasks::reset();
        $this->assertCount(2, MyTasks::forCurrent());
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `--filter MyTasksTest`
Expected: FAIL — `Class "SkazResidents\Service\MyTasks" not found`.

- [ ] **Step 3: Write minimal implementation** — создать `residents/src/Service/MyTasks.php`:

```php
<?php
declare(strict_types=1);
namespace SkazResidents\Service;

use SkazResidents\{Auth, Sections};
use SkazResidents\Repository\{
    ToolLoanRepository, BookLoanRepository, TripBookingRepository,
    ProductRepository, DiaryRepository, PurchaseRepository, FamilyRepository
};

/**
 * «Мои дела» — всё, что ждёт действия жителя, собранное из текущих статусов.
 * Спека: docs/superpowers/specs/2026-09-24-moi-dela-design.md.
 *
 * Отдельной таблицы нет: дело существует, пока существует и стоит в нужном
 * статусе сама заявка, объявление или закупка. Поэтому список не расходится с
 * реальностью и не зависит ни от почты, ни от бота — ради этого он и заведён:
 * у жителей из Telegram и MAX почты нет, а бот пишет только тем, кто разрешил.
 *
 * Дело — массив ['kind', 'title', 'detail', 'link', 'date', 'urgent'].
 * date — момент, с которого дело ждёт; по нему сортируем.
 */
final class MyTasks
{
    /** Результат для вошедшего жителя на время запроса — см. forCurrent(). */
    private static ?array $current = null;

    public function __construct(
        private ToolLoanRepository $toolLoans = new ToolLoanRepository(),
        private BookLoanRepository $bookLoans = new BookLoanRepository(),
        private TripBookingRepository $bookings = new TripBookingRepository(),
        private ProductRepository $products = new ProductRepository(),
        private DiaryRepository $diary = new DiaryRepository(),
        private PurchaseRepository $purchases = new PurchaseRepository(),
        private FamilyRepository $families = new FamilyRepository()
    ) {}

    /**
     * Дела вошедшего жителя — один расчёт на запрос: главная, блок, меню и
     * страница «Мои дела» берут один и тот же список. Любой сбой — пустой
     * список и запись в лог: число в меню есть на каждой странице, и из-за дел
     * не должна падать ни одна.
     *
     * @return array<int,array<string,mixed>>
     */
    public static function forCurrent(): array
    {
        if (self::$current !== null) { return self::$current; }
        $id = Auth::id();
        if ($id === null) { return []; }
        try {
            self::$current = (new self())->build($id, Auth::isEditor(), date('Y-m-d'));
        } catch (\Throwable $e) {
            error_log('MyTasks: ' . $e->getMessage());
            self::$current = [];
        }
        return self::$current;
    }

    /** Забыть запомненный результат (тесты; в вебе запрос и так живёт один раз). */
    public static function reset(): void
    {
        self::$current = null;
    }

    /** @return array<int,array<string,mixed>> */
    public function build(int $familyId, bool $isEditor, string $today): array
    {
        $tasks = [
            ...$this->collect('instrumenty', fn(): array => $this->toolRequests($familyId)),
        ];
        return self::sort($tasks);
    }

    /**
     * Один источник дел. Выключенный раздел не опрашивается вовсе; сбой
     * источника — в лог, остальные дела показываем.
     *
     * @param callable():array<int,array<string,mixed>> $source
     * @return array<int,array<string,mixed>>
     */
    private function collect(?string $section, callable $source): array
    {
        if ($section !== null && !Sections::isEnabled($section)) { return []; }
        try {
            return $source();
        } catch (\Throwable $e) {
            error_log('MyTasks (' . ($section ?? 'moderation') . '): ' . $e->getMessage());
            return [];
        }
    }

    /** @return array<int,array<string,mixed>> */
    private function toolRequests(int $me): array
    {
        $out = [];
        foreach ($this->toolLoans->listIncoming($me, ['requested']) as $l) {
            $out[] = self::task('tool_request', 'Заявка на «' . $l['tool_name'] . '»',
                self::withDue((string) $l['borrower_name'], $l['due_date'] ?? null),
                '/poselenie/instrumenty/moi', (string) $l['requested_at']);
        }
        return $out;
    }

    /**
     * Срочные — сверху; дальше кто дольше ждёт, тот выше; сводка модерации —
     * последней: это не одно дело, а счётчик очереди.
     *
     * @param array<int,array<string,mixed>> $tasks
     * @return array<int,array<string,mixed>>
     */
    private static function sort(array $tasks): array
    {
        $rank = static fn(array $t): int => $t['kind'] === 'moderation' ? 2 : ($t['urgent'] ? 0 : 1);
        usort($tasks, static fn(array $a, array $b): int => [$rank($a), $a['date']] <=> [$rank($b), $b['date']]);
        return $tasks;
    }

    /** @return array<string,mixed> */
    private static function task(string $kind, string $title, string $detail, string $link, string $date, bool $urgent = false): array
    {
        return ['kind' => $kind, 'title' => $title, 'detail' => $detail, 'link' => $link, 'date' => $date, 'urgent' => $urgent];
    }

    /** «Семья Руденко · до 1 октября 2026» — кто просит и к какому сроку. */
    private static function withDue(string $who, ?string $due): string
    {
        return ($due ?? '') !== '' ? $who . ' · до ' . ru_date($due) : $who;
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `--filter MyTasksTest`
Expected: PASS, 4 теста.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: сборщик MyTasks и заявки на инструменты

Каркас списка дел, собираемого из текущих статусов: одна форма дела,
сортировка (срочные сверху, дольше ждущие выше), выключатели разделов,
изоляция сбоев источников, один расчёт на запрос. Первый источник — заявки
на инструменты жителя.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 4: Заявки на книги и брони поездок

**Files:**
- Modify: `residents/src/Service/MyTasks.php`
- Test: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing tests** — добавить в `MyTasksTest`:

```php
    public function test_request_for_my_book_waits_until_decided(): void
    {
        $book = (new BookRepository())->create($this->me, 'Анастасия', 'В. Мегре', 'Проза', null, null, self::NOW);
        $loans = new BookLoanRepository();
        $loan = $loans->create($book, $this->neighbour, null, null, self::NOW);

        $t = $this->tasks();
        $this->assertSame(['book_request'], array_column($t, 'kind'));
        $this->assertSame('Бронь книги «Анастасия»', $t[0]['title']);
        $this->assertSame('Семья Руденко', $t[0]['detail']);
        $this->assertSame('/poselenie/knigi/moi', $t[0]['link']);

        $loans->decline($loan, self::NOW);
        $this->assertSame([], $this->kinds());
    }

    public function test_booking_on_my_trip_waits_until_decided(): void
    {
        $trip = (new TripRepository())->create($this->me, 'Терем', 'Краснодар', '2026-09-30', '09:00', 3, null, self::NOW);
        $bookings = new TripBookingRepository();
        $b = $bookings->create($trip, $this->neighbour, 2, null, self::NOW);

        $t = $this->tasks();
        $this->assertSame(['trip_booking'], array_column($t, 'kind'));
        $this->assertSame('Бронь в поездке Терем → Краснодар', $t[0]['title']);
        $this->assertSame('Семья Руденко, мест: 2 · 30 сентября 2026', $t[0]['detail']);
        $this->assertSame('/poselenie/poezdki/moi', $t[0]['link']);

        $bookings->setStatus($b, 'confirmed', self::NOW);
        $this->assertSame([], $this->kinds());
    }

    public function test_booking_on_past_or_cancelled_trip_is_not_a_task(): void
    {
        $trips = new TripRepository();
        $past = $trips->create($this->me, 'А', 'Б', '2026-09-01', null, 3, null, self::NOW);
        $cancelled = $trips->create($this->me, 'В', 'Г', '2026-10-05', null, 3, null, self::NOW);
        $trips->setStatus($cancelled, 'cancelled');
        $bookings = new TripBookingRepository();
        $bookings->create($past, $this->neighbour, 1, null, self::NOW);
        $bookings->create($cancelled, $this->neighbour, 1, null, self::NOW);

        $this->assertSame([], $this->kinds());
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter MyTasksTest`
Expected: FAIL — в трёх новых тестах пустой список вместо `book_request` / `trip_booking` (третий тест пройдёт — это нормально, он страхует от регресса).

- [ ] **Step 3: Write minimal implementation** — в `MyTasks::build()` расширить список:

```php
        $tasks = [
            ...$this->collect('instrumenty', fn(): array => $this->toolRequests($familyId)),
            ...$this->collect('knigi',       fn(): array => $this->bookRequests($familyId)),
            ...$this->collect('poezdki',     fn(): array => $this->tripBookings($familyId, $today)),
        ];
```

и добавить методы после `toolRequests()`:

```php
    /** @return array<int,array<string,mixed>> */
    private function bookRequests(int $me): array
    {
        $out = [];
        foreach ($this->bookLoans->listIncoming($me, ['requested']) as $l) {
            $out[] = self::task('book_request', 'Бронь книги «' . $l['book_title'] . '»',
                self::withDue((string) $l['borrower_name'], $l['due_date'] ?? null),
                '/poselenie/knigi/moi', (string) $l['requested_at']);
        }
        return $out;
    }

    /**
     * Брони в поездках жителя. Отмена поездки брони не гасит, поэтому
     * отсекаем по статусу самой поездки и по её дате.
     *
     * @return array<int,array<string,mixed>>
     */
    private function tripBookings(int $me, string $today): array
    {
        $out = [];
        foreach ($this->bookings->listIncoming($me, ['requested']) as $b) {
            if (($b['trip_status'] ?? '') !== 'active' || (string) $b['trip_date'] < $today) { continue; }
            $out[] = self::task('trip_booking', 'Бронь в поездке ' . $b['origin'] . ' → ' . $b['destination'],
                $b['passenger_name'] . ', мест: ' . (int) $b['seats'] . ' · ' . ru_date((string) $b['trip_date']),
                '/poselenie/poezdki/moi', (string) $b['created_at']);
        }
        return $out;
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter MyTasksTest`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: заявки на книги и брони поездок

Брони в прошедших и отменённых поездках делом не считаются: отмена поездки
брони не гасит, поэтому смотрим на статус и дату самой поездки.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 5: Отклонённые объявления и записи дневника

**Files:**
- Modify: `residents/src/Service/MyTasks.php`
- Test: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing tests** — добавить в `MyTasksTest`:

```php
    public function test_rejected_listing_waits_until_resubmitted(): void
    {
        $products = new ProductRepository();
        $id = $products->create($this->me, 'Мёд', 'Майский', '500', '@me', self::NOW);
        $products->reject($id, 'Нет цены за единицу');

        $t = $this->tasks();
        $this->assertSame(['product_rejected'], array_column($t, 'kind'));
        $this->assertSame('Объявление «Мёд» не прошло проверку', $t[0]['title']);
        $this->assertSame('Причина: Нет цены за единицу', $t[0]['detail']);
        $this->assertSame('/poselenie/yarmarka/' . $id . '/redaktirovat', $t[0]['link']);

        // Правка возвращает объявление на проверку — дело снято.
        $products->update($id, 'Мёд', 'Майский', '500', '@me', self::NOW, 'public', 'кг.');
        $this->assertSame([], $this->kinds());
    }

    public function test_rejected_diary_entry_without_reason_says_what_to_do(): void
    {
        $diary = new DiaryRepository();
        $id = $diary->create($this->me, 'Как копали пруд', 'текст', 'public', self::NOW);
        $diary->reject($id, 'Без указания причины');   // так пишет модерация при пустой причине

        $t = $this->tasks();
        $this->assertSame(['diary_rejected'], array_column($t, 'kind'));
        $this->assertSame('Запись «Как копали пруд» не прошла проверку', $t[0]['title']);
        $this->assertSame('Исправьте и отправьте снова', $t[0]['detail']);
        $this->assertSame('/poselenie/dnevnik/' . $id . '/redaktirovat', $t[0]['link']);

        $diary->update($id, 'Как копали пруд', 'текст', 'public', self::NOW);
        $this->assertSame([], $this->kinds());
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter MyTasksTest`
Expected: FAIL — пустой список вместо `product_rejected` / `diary_rejected`.

- [ ] **Step 3: Write minimal implementation** — в `build()` добавить в конец массива `$tasks`:

```php
            ...$this->collect('yarmarka',    fn(): array => $this->rejectedProducts($familyId)),
            ...$this->collect('dnevniki',    fn(): array => $this->rejectedDiary($familyId)),
```

методы — после `tripBookings()`:

```php
    /** @return array<int,array<string,mixed>> */
    private function rejectedProducts(int $me): array
    {
        $out = [];
        foreach ($this->products->listByFamily($me) as $p) {
            if ($p['status'] !== 'rejected') { continue; }
            $out[] = self::task('product_rejected', 'Объявление «' . $p['title'] . '» не прошло проверку',
                self::reason($p['reject_reason'] ?? null),
                '/poselenie/yarmarka/' . (int) $p['id'] . '/redaktirovat', (string) $p['updated_at']);
        }
        return $out;
    }

    /** @return array<int,array<string,mixed>> */
    private function rejectedDiary(int $me): array
    {
        $out = [];
        foreach ($this->diary->listByFamily($me) as $e) {
            if ($e['status'] !== 'rejected') { continue; }
            $out[] = self::task('diary_rejected', 'Запись «' . $e['title'] . '» не прошла проверку',
                self::reason($e['reject_reason'] ?? null),
                '/poselenie/dnevnik/' . (int) $e['id'] . '/redaktirovat', (string) $e['updated_at']);
        }
        return $out;
    }
```

и помощник — после `withDue()`:

```php
    /**
     * Причина отказа модератора; без неё — что делать дальше. «Без указания
     * причины» пишет сама модерация, когда поле оставили пустым.
     */
    private static function reason(?string $reason): string
    {
        $reason = trim((string) $reason);
        return ($reason !== '' && $reason !== 'Без указания причины')
            ? 'Причина: ' . $reason
            : 'Исправьте и отправьте снова';
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter MyTasksTest`
Expected: PASS, 9 тестов.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: отклонённые объявления и записи дневника

С причиной отказа и ссылкой на правку. Правка возвращает на проверку —
дело снимается само.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 6: «Пора вернуть» — просроченные инструменты и книги

**Files:**
- Modify: `residents/src/Service/MyTasks.php`
- Test: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing tests** — добавить в `MyTasksTest`:

```php
    public function test_overdue_borrowed_tool_is_urgent_from_the_next_day(): void
    {
        $tool = (new ToolRepository())->create($this->neighbour, 'Пила', 'Сад', null, null, null, self::NOW);
        $loans = new ToolLoanRepository();
        $loan = $loans->create($tool, $this->me, null, self::TODAY, self::NOW);
        $loans->give($loan, self::NOW);

        // В сам день срока — ещё не дело.
        $this->assertSame([], $this->kinds());

        $t = $this->tasks(false, '2026-09-25');
        $this->assertSame(['tool_overdue'], array_column($t, 'kind'));
        $this->assertTrue($t[0]['urgent']);
        $this->assertSame('Пора вернуть «Пила»', $t[0]['title']);
        $this->assertSame('Семья Руденко · срок был 24 сентября 2026', $t[0]['detail']);
        $this->assertSame('/poselenie/instrumenty/moi', $t[0]['link']);

        $loans->markReturned($loan, 'ok', null, self::NOW);
        $this->assertSame([], $this->kinds(false, '2026-09-25'));
    }

    public function test_overdue_borrowed_book_is_urgent(): void
    {
        $book = (new BookRepository())->create($this->neighbour, 'Звенящие кедры', 'В. Мегре', 'Проза', null, null, self::NOW);
        $loans = new BookLoanRepository();
        $loan = $loans->create($book, $this->me, null, '2026-09-10', self::NOW);
        $loans->give($loan, self::NOW);

        $t = $this->tasks();
        $this->assertSame(['book_overdue'], array_column($t, 'kind'));
        $this->assertTrue($t[0]['urgent']);
        $this->assertSame('Пора вернуть книгу «Звенящие кедры»', $t[0]['title']);
        $this->assertSame('/poselenie/knigi/moi', $t[0]['link']);
    }

    public function test_urgent_first_then_longest_waiting(): void
    {
        $tools = new ToolRepository();
        $loans = new ToolLoanRepository();
        $loans->create($tools->create($this->me, 'Новая', 'x', null, null, null, self::NOW), $this->neighbour, null, null, '2026-09-22 10:00:00');
        $loans->create($tools->create($this->me, 'Старая', 'x', null, null, null, self::NOW), $this->neighbour, null, null, '2026-09-10 10:00:00');
        $borrowed = $loans->create($tools->create($this->neighbour, 'Чужая', 'x', null, null, null, self::NOW), $this->me, null, '2026-09-23', self::NOW);
        $loans->give($borrowed, self::NOW);

        $this->assertSame(
            ['Пора вернуть «Чужая»', 'Заявка на «Старая»', 'Заявка на «Новая»'],
            array_column($this->tasks(), 'title')
        );
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter MyTasksTest`
Expected: FAIL — нет `tool_overdue` / `book_overdue`; в тесте порядка нет строки «Пора вернуть».

- [ ] **Step 3: Write minimal implementation** — в `build()` добавить в конец массива `$tasks`:

```php
            ...$this->collect('instrumenty', fn(): array => $this->overdueTools($familyId, $today)),
            ...$this->collect('knigi',       fn(): array => $this->overdueBooks($familyId, $today)),
```

методы — после `rejectedDiary()`:

```php
    /**
     * Взятое у соседа, у которого прошёл желаемый срок. «Прошёл» — со
     * следующего дня: в сам день срока ещё не просрочено.
     *
     * @return array<int,array<string,mixed>>
     */
    private function overdueTools(int $me, string $today): array
    {
        $out = [];
        foreach ($this->toolLoans->listByBorrower($me) as $l) {
            $due = (string) ($l['due_date'] ?? '');
            if ($l['status'] !== 'on_loan' || $due === '' || $due >= $today) { continue; }
            $out[] = self::task('tool_overdue', 'Пора вернуть «' . $l['tool_name'] . '»',
                $l['owner_name'] . ' · срок был ' . ru_date($due),
                '/poselenie/instrumenty/moi', $due, true);
        }
        return $out;
    }

    /** @return array<int,array<string,mixed>> */
    private function overdueBooks(int $me, string $today): array
    {
        $out = [];
        foreach ($this->bookLoans->listByBorrower($me) as $l) {
            $due = (string) ($l['due_date'] ?? '');
            if ($l['status'] !== 'on_loan' || $due === '' || $due >= $today) { continue; }
            $out[] = self::task('book_overdue', 'Пора вернуть книгу «' . $l['book_title'] . '»',
                $l['owner_name'] . ' · срок был ' . ru_date($due),
                '/poselenie/knigi/moi', $due, true);
        }
        return $out;
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter MyTasksTest`
Expected: PASS, 12 тестов.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: пора вернуть взятое у соседа

Инструменты и книги, у которых прошёл желаемый срок, — срочные дела,
показываются первыми. Просрочка считается со следующего дня после срока.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 7: Закупки — организатору и участнику

**Files:**
- Modify: `residents/src/Service/MyTasks.php`
- Test: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing tests** — добавить в `MyTasksTest`:

```php
    public function test_organizer_is_asked_to_order_after_deadline(): void
    {
        $purchases = new PurchaseRepository();
        $id = $purchases->create($this->me, 'Мёд', null, 'кг', '450', null, '2026-09-20', null, 'Общий дом', null, self::NOW);

        $t = $this->tasks();
        $this->assertSame(['purchase_deadline'], array_column($t, 'kind'));
        $this->assertSame('Сбор по закупке «Мёд» закончился', $t[0]['title']);
        $this->assertSame('Оформите заказ или продлите срок · срок был 20 сентября 2026', $t[0]['detail']);
        $this->assertSame('/poselenie/zakupki/' . $id, $t[0]['link']);

        $purchases->setStatus($id, 'ordered', self::NOW);
        $this->assertSame([], $this->kinds());
    }

    public function test_arrived_purchase_is_closed_for_both_sides_by_one_payment_mark(): void
    {
        $purchases = new PurchaseRepository();
        $orders = new PurchaseOrderRepository();
        $id = $purchases->create($this->neighbour, 'Мёд', null, 'кг', '450', null, null, null, 'Общий дом', null, self::NOW);
        $mine = $orders->place($id, $this->me, '3', null, self::NOW);
        $purchases->setStatus($id, 'arrived', self::NOW);

        // Участнику — забрать.
        $t = $this->tasks();
        $this->assertSame(['purchase_pickup'], array_column($t, 'kind'));
        $this->assertSame('Привезли «Мёд» — заберите', $t[0]['title']);
        $this->assertSame('Где забрать: Общий дом', $t[0]['detail']);

        // Организатору — отметить оплату.
        $org = $this->tasks(false, self::TODAY, $this->neighbour);
        $this->assertSame(['purchase_unpaid'], array_column($org, 'kind'));
        $this->assertSame('Отметьте оплату: «Мёд»', $org[0]['title']);
        $this->assertSame('Не отмечено у 1 участника', $org[0]['detail']);

        // Одна отметка оплаты закрывает оба дела.
        $orders->setPaid($mine, true, self::NOW);
        $this->assertSame([], $this->kinds());
        $this->assertSame([], $this->kinds(false, self::TODAY, $this->neighbour));
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter MyTasksTest`
Expected: FAIL — нет `purchase_deadline` / `purchase_pickup` / `purchase_unpaid`.

- [ ] **Step 3: Write minimal implementation** — в `build()` добавить в конец массива `$tasks`:

```php
            ...$this->collect('zakupki',     fn(): array => $this->purchasesAsOrganizer($familyId, $today)),
            ...$this->collect('zakupki',     fn(): array => $this->purchasesAsParticipant($familyId)),
```

методы — после `overdueBooks()`:

```php
    /**
     * Закупки, которые ведёт житель: сбор закончился, а закупка всё ещё
     * «идёт сбор»; привезли, а у кого-то не отмечена оплата.
     *
     * @return array<int,array<string,mixed>>
     */
    private function purchasesAsOrganizer(int $me, string $today): array
    {
        $out = [];
        foreach ($this->purchases->listByOrganizer($me) as $p) {
            $link = '/poselenie/zakupki/' . (int) $p['id'];
            $deadline = (string) ($p['deadline'] ?? '');
            if ($p['status'] === 'collecting' && $deadline !== '' && $deadline < $today) {
                $out[] = self::task('purchase_deadline', 'Сбор по закупке «' . $p['title'] . '» закончился',
                    'Оформите заказ или продлите срок · срок был ' . ru_date($deadline), $link, $deadline);
            }
            $unpaid = (int) ($p['unpaid_count'] ?? 0);
            if ($p['status'] === 'arrived' && $unpaid > 0) {
                $out[] = self::task('purchase_unpaid', 'Отметьте оплату: «' . $p['title'] . '»',
                    'Не отмечено у ' . $unpaid . ' ' . plural_ru($unpaid, 'участника', 'участников', 'участников'),
                    $link, (string) $p['updated_at']);
            }
        }
        return $out;
    }

    /**
     * Чужие закупки, где житель участник: привезли — забрать. Закрывается той
     * же отметкой оплаты, что и дело организатора: иначе забравший всё
     * житель видел бы дело, пока закупку не переведут в «завершена».
     *
     * @return array<int,array<string,mixed>>
     */
    private function purchasesAsParticipant(int $me): array
    {
        $out = [];
        foreach ($this->purchases->listByParticipant($me) as $p) {
            if ($p['status'] !== 'arrived' || ($p['my_paid_at'] ?? null) !== null || (int) $p['organizer_id'] === $me) { continue; }
            $pickup = trim((string) ($p['pickup'] ?? ''));
            $out[] = self::task('purchase_pickup', 'Привезли «' . $p['title'] . '» — заберите',
                $pickup !== '' ? 'Где забрать: ' . $pickup : 'Уточните у организатора: ' . $p['organizer_name'],
                '/poselenie/zakupki/' . (int) $p['id'], (string) $p['updated_at']);
        }
        return $out;
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter MyTasksTest`
Expected: PASS, 14 тестов.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: закупки — организатору и участнику

Организатору: сбор закончился — оформить заказ; привезли — отметить оплату.
Участнику: привезли — забрать, с местом выдачи. «Забрать» и «отметить
оплату» закрываются одним действием — отметкой оплаты.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 8: Очередь модерации и устойчивость к сбоям

**Files:**
- Modify: `residents/src/Service/MyTasks.php`
- Test: `residents/tests/MyTasksTest.php`

- [ ] **Step 1: Write the failing tests** — добавить в `MyTasksTest`:

```php
    public function test_moderation_queue_is_for_editors_only_and_goes_last(): void
    {
        (new ProductRepository())->create($this->neighbour, 'Яйца', 'Домашние', '120', '@n', self::NOW);  // «на сайте» → на проверку
        (new DiaryRepository())->create($this->neighbour, 'Запись', 'текст', 'public', self::NOW);
        $tool = (new ToolRepository())->create($this->me, 'Дрель', 'Электро', null, null, null, self::NOW);
        (new ToolLoanRepository())->create($tool, $this->neighbour, null, null, self::NOW);

        $this->assertSame(['tool_request'], $this->kinds(false));

        // Обе семьи из setUp созданы как «pending» — это и есть две заявки на вход.
        $t = $this->tasks(true);
        $this->assertSame(['tool_request', 'moderation'], array_column($t, 'kind'));
        $this->assertSame('На проверке: 4', $t[1]['title']);
        $this->assertSame('заявки на вход: 2 · записи: 1 · объявления: 1', $t[1]['detail']);
        $this->assertSame('/poselenie/moderation', $t[1]['link']);
    }

    public function test_empty_moderation_queue_is_not_a_task(): void
    {
        $fam = new FamilyRepository();
        $fam->approve($this->me, self::NOW);
        $fam->approve($this->neighbour, self::NOW);

        $this->assertSame([], $this->kinds(true));
    }

    public function test_broken_source_does_not_hide_the_rest(): void
    {
        $tool = (new ToolRepository())->create($this->me, 'Дрель', 'Электро', null, null, null, self::NOW);
        (new ToolLoanRepository())->create($tool, $this->neighbour, null, null, self::NOW);
        Database::pdo()->exec('DROP TABLE purchase_orders');   // оба источника закупок падают

        $this->assertSame(['tool_request'], $this->kinds());
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter MyTasksTest`
Expected: FAIL — в первом тесте нет строки `moderation` (второй и третий пройдут: пустая очередь и изоляция сбоев уже есть в каркасе).

- [ ] **Step 3: Write minimal implementation** — в `build()` после массива `$tasks`, перед `return`:

```php
        if ($isEditor) {
            $tasks = [...$tasks, ...$this->collect(null, fn(): array => $this->moderationQueue())];
        }
```

метод — после `purchasesAsParticipant()`:

```php
    /**
     * Сводка очереди модерации — одной строкой, только редактору и админу.
     * В пояснении — только непустые части.
     *
     * @return array<int,array<string,mixed>>
     */
    private function moderationQueue(): array
    {
        $parts = array_filter([
            'заявки на вход' => count($this->families->listByStatus('pending')),
            'записи'         => count($this->diary->listPending()),
            'объявления'     => count($this->products->listPending()),
        ]);
        $total = array_sum($parts);
        if ($total === 0) { return []; }
        $detail = implode(' · ', array_map(
            static fn(string $what, int $n): string => $what . ': ' . $n,
            array_keys($parts), $parts
        ));
        return [self::task('moderation', 'На проверке: ' . $total, $detail, '/poselenie/moderation', '')];
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter MyTasksTest`
Expected: PASS, 17 тестов.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Service/MyTasks.php residents/tests/MyTasksTest.php && git commit -F - <<'MSG'
«Мои дела»: очередь модерации для редакторов

Одна строка-сводка — заявки на вход, записи и объявления на проверке, —
всегда последней. Плюс проверка, что сломанный источник не прячет остальные
дела.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 9: Страница «Мои дела»

**Files:**
- Create: `residents/src/Controller/TasksController.php`
- Create: `residents/src/templates/tasks/index.php`
- Modify: `residents/public/index.php`
- Test: `residents/tests/TasksPageTest.php`

- [ ] **Step 1: Write the failing test** — создать `residents/tests/TasksPageTest.php`:

```php
<?php
declare(strict_types=1);
namespace SkazResidents\Tests;
use PHPUnit\Framework\TestCase;

/** Страница «Мои дела»: список дел и пустое состояние. */
final class TasksPageTest extends TestCase
{
    /** @param array<int,array<string,mixed>> $tasks */
    private function render(array $tasks): string
    {
        ob_start();
        require __DIR__ . '/../src/templates/tasks/index.php';
        return (string) ob_get_clean();
    }

    public function test_lists_tasks_with_links_and_marks_urgent(): void
    {
        $html = $this->render([
            ['kind' => 'tool_overdue', 'title' => 'Пора вернуть «Пила»', 'detail' => 'Семья Руденко · срок был 24 сентября 2026',
             'link' => '/poselenie/instrumenty/moi', 'date' => '2026-09-24', 'urgent' => true],
            ['kind' => 'tool_request', 'title' => 'Заявка на «Дрель»', 'detail' => 'Семья Руденко',
             'link' => '/poselenie/instrumenty/moi', 'date' => '2026-09-20 10:00:00', 'urgent' => false],
        ]);
        $this->assertStringContainsString('<h1>Мои дела</h1>', $html);
        $this->assertSame(2, substr_count($html, 'class="task-item'));
        $this->assertSame(1, substr_count($html, 'task-item--urgent'));
        $this->assertStringContainsString('Пора вернуть «Пила»', $html);
        $this->assertStringContainsString('href="/poselenie/instrumenty/moi"', $html);
    }

    public function test_empty_state_says_everything_is_done(): void
    {
        $html = $this->render([]);
        $this->assertStringContainsString('Все дела сделаны', $html);
        $this->assertStringNotContainsString('task-item', $html);
    }

    public function test_titles_are_escaped(): void
    {
        $html = $this->render([['kind' => 'tool_request', 'title' => 'Заявка на «<b>x</b>»', 'detail' => '',
            'link' => '/poselenie/instrumenty/moi', 'date' => '', 'urgent' => false]]);
        $this->assertStringNotContainsString('<b>x</b>', $html);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `--filter TasksPageTest`
Expected: FAIL — `Failed to open stream: …/templates/tasks/index.php`.

- [ ] **Step 3: Write minimal implementation**

Создать `residents/src/templates/tasks/index.php`:

```php
<?php use SkazResidents\View; /** @var array<int,array<string,mixed>> $tasks */ ?>
<?php $backFallback = '/poselenie/app'; require __DIR__ . '/../partials/back.php'; ?>
<h1>Мои дела</h1>
<?php if (!$tasks): ?>
    <p class="res-meta tool-empty">Все дела сделаны. <a href="/poselenie/app">На главную</a></p>
<?php else: ?>
    <p class="res-meta">То, что ждёт вашего ответа. Сделаете — пункт исчезнет сам.</p>
    <ul class="task-list">
        <?php foreach ($tasks as $t): ?>
            <li>
                <a class="task-item<?= $t['urgent'] ? ' task-item--urgent' : '' ?>" href="<?= View::e($t['link']) ?>">
                    <b><?= View::e($t['title']) ?></b>
                    <?php if ($t['detail'] !== ''): ?><span><?= View::e($t['detail']) ?></span><?php endif; ?>
                </a>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>
```

Создать `residents/src/Controller/TasksController.php`:

```php
<?php
declare(strict_types=1);
namespace SkazResidents\Controller;

use SkazResidents\{Auth, View};
use SkazResidents\Service\MyTasks;

/**
 * «Мои дела» — всё, что ждёт действия жителя: заявки на его инструменты и
 * книги, брони его поездок, отказы модерации, просроченный возврат, закупки.
 * Собирается из текущих статусов, см. {@see MyTasks}.
 */
final class TasksController
{
    public function index(): void
    {
        Auth::requireLogin();
        View::render('tasks/index', ['tasks' => MyTasks::forCurrent()], 'Мои дела');
    }
}
```

В `residents/public/index.php` добавить импорт рядом с остальными `use SkazResidents\Controller\…;`:

```php
use SkazResidents\Controller\TasksController;
```

и маршрут сразу после строки `$router->get('/poselenie/app', [$app, 'home']);`:

```php
$router->get('/poselenie/dela', [new TasksController(), 'index']);   // «Мои дела»
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter "TasksPageTest|TopNavigationTest"`
Expected: PASS — `TasksPageTest` (3) и `TopNavigationTest` (новая страница подключает общий партиал навигации).

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Controller/TasksController.php residents/src/templates/tasks/index.php residents/public/index.php residents/tests/TasksPageTest.php && git commit -F - <<'MSG'
«Мои дела»: страница /poselenie/dela

Список дел со ссылками туда, где дело делается; срочные выделены. Пусто —
«Все дела сделаны».

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 10: Блок «Ждут вас» на главной

**Files:**
- Modify: `residents/src/Controller/AppController.php`
- Modify: `residents/src/templates/app/home.php` (перед блоком дневника, строка с комментарием «Блок дневников — единственная ссылка…»)
- Test: `residents/tests/AppHomeTilesTest.php`

- [ ] **Step 1: Write the failing tests** — в `AppHomeTilesTest` заменить метод `render()` на версию с делами и добавить два теста:

```php
    /** @param array<int,array<string,mixed>> $tasks */
    private function render(array $tasks = []): string
    {
        $dash = (new AppDashboard())->build(1);
        $me = 'Поместье «АгудариЯ»';
        $savedAt = '10:00';
        ob_start();
        require __DIR__ . '/../src/templates/app/home.php';
        return (string) ob_get_clean();
    }

    public function test_tasks_block_shows_count_and_first_two_titles(): void
    {
        $task = static fn(string $title): array => ['kind' => 'tool_request', 'title' => $title, 'detail' => '',
            'link' => '/poselenie/instrumenty/moi', 'date' => '', 'urgent' => false];
        $html = $this->render([$task('Заявка на «Дрель»'), $task('Заявка на «Пилу»'), $task('Заявка на «Лопату»')]);

        $this->assertStringContainsString('<a class="app-tasks" href="/poselenie/dela">', $html);
        $this->assertStringContainsString('Ждут вас: 3 дела', $html);
        $this->assertStringContainsString('Заявка на «Дрель» · Заявка на «Пилу»', $html);
        $this->assertStringNotContainsString('Лопату', $html);
        // Блок стоит первым — над дневником.
        $this->assertLessThan(strpos($html, 'class="app-diary"'), strpos($html, 'class="app-tasks"'));
    }

    public function test_no_tasks_no_block(): void
    {
        $this->assertStringNotContainsString('app-tasks', $this->render([]));
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter AppHomeTilesTest`
Expected: FAIL — в `test_tasks_block_shows_count_and_first_two_titles` нет `app-tasks`.

- [ ] **Step 3: Write minimal implementation**

В `residents/src/templates/app/home.php` вставить **перед** строкой `  <?php` комментария «Блок дневников — единственная ссылка на раздел с главной…»:

```php
  <?php $tasks = $tasks ?? []; ?>
  <?php if ($tasks): /* «Мои дела» — только когда есть что делать; нет дел — блока нет. */ ?>
    <a class="app-tasks" href="/poselenie/dela">
      <b>Ждут вас: <?= count($tasks) ?> <?= View::e(plural_ru(count($tasks), 'дело', 'дела', 'дел')) ?></b>
      <span><?= View::e(implode(' · ', array_map(static fn(array $t): string => (string) $t['title'], array_slice($tasks, 0, 2)))) ?></span>
    </a>
  <?php endif; ?>

```

В `residents/src/Controller/AppController.php` добавить импорт после `use SkazResidents\Service\AppDashboard;`:

```php
use SkazResidents\Service\MyTasks;
```

и в `home()` в массив `View::render('app/home', [ … ])` строку после `'savedAt' => date('H:i'),`:

```php
            'tasks'   => MyTasks::forCurrent(),
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter AppHomeTilesTest`
Expected: PASS, все тесты класса.

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/Controller/AppController.php residents/src/templates/app/home.php residents/tests/AppHomeTilesTest.php && git commit -F - <<'MSG'
Главная: блок «Ждут вас» с делами жителя

Первым, над блоком дневника: число дел и два первых заголовка, весь блок
ведёт на «Мои дела». Нет дел — блока нет, раскладка плиток 4×2 не меняется.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 11: Пункт меню «Мои дела» с числом

**Files:**
- Modify: `residents/src/templates/layout.php` (сразу после открывающего `<nav class="res-nav">`)
- Test: `residents/tests/LayoutMenuTest.php`

- [ ] **Step 1: Write the failing tests** — в `LayoutMenuTest`:

в импорты добавить

```php
use SkazResidents\Service\MyTasks;
use SkazResidents\Repository\{FamilyRepository, ToolRepository, ToolLoanRepository};
```

(если строка `use SkazResidents\Repository\SectionSettingsRepository;` уже есть — объединить: `use SkazResidents\Repository\{SectionSettingsRepository, FamilyRepository, ToolRepository, ToolLoanRepository};`);

в `setUp()` после `Sections::reset();` добавить

```php
        MyTasks::reset();   // дела запоминаются на запрос — между тестами их надо забывать
```

и тесты:

```php
    public function test_resident_menu_has_tasks_with_count(): void
    {
        $fam = new FamilyRepository();
        $me = $fam->createPending('me@skaz-kray.ru', 'H', 'Я');
        $nei = $fam->createPending('nei@skaz-kray.ru', 'H', 'Сосед');
        $tool = (new ToolRepository())->create($me, 'Дрель', 'x', null, null, null, '2026-09-20 10:00:00');
        (new ToolLoanRepository())->create($tool, $nei, null, null, '2026-09-20 10:00:00');
        $_SESSION['family_id'] = $me;

        $html = $this->render();
        $this->assertContains('/poselenie/dela', $this->menu($html));
        $this->assertStringContainsString('Мои дела <span class="res-nav-count">1</span>', $html);
    }

    public function test_no_count_when_nothing_to_do(): void
    {
        $_SESSION['family_id'] = 1;
        $html = $this->render();
        $this->assertContains('/poselenie/dela', $this->menu($html));
        $this->assertStringNotContainsString('res-nav-count', $html);
    }

    public function test_guest_has_no_tasks_item(): void
    {
        $this->assertNotContains('/poselenie/dela', $this->menu($this->render()));
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `--filter LayoutMenuTest`
Expected: FAIL — в меню нет `/poselenie/dela`.

- [ ] **Step 3: Write minimal implementation** — в `residents/src/templates/layout.php` сразу после строки `    <nav class="res-nav">` вставить:

```php
        <?php if (Auth::id() !== null): ?>
            <?php /* Первым: это то, что ждёт ответа. Число — на всех страницах (MyTasks
                     считает один раз за запрос и при сбое отдаёт пустой список). */ ?>
            <?php $taskCount = count(\SkazResidents\Service\MyTasks::forCurrent()); ?>
            <a href="/poselenie/dela">Мои дела<?php if ($taskCount > 0): ?> <span class="res-nav-count"><?= $taskCount ?></span><?php endif; ?></a>
        <?php endif; ?>
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `--filter LayoutMenuTest`
Expected: PASS, все тесты класса (старые про «Ярмарку» тоже).

- [ ] **Step 5: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/src/templates/layout.php residents/tests/LayoutMenuTest.php && git commit -F - <<'MSG'
Меню: пункт «Мои дела» с числом дел

Первым в меню вошедшего жителя, число — на всех страницах, если оно больше
нуля. Список считается один раз за запрос.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 12: Стили

**Files:**
- Modify: `residents/public/assets/residents.css` (после правила `.app-diary--other b`)

Проверяемого поведения нет — только внешний вид; тест не пишем, проверяем глазами после деплоя.

- [ ] **Step 1: Добавить стили** — после строки `.app-diary--other b { color: #036844; }`:

```css
/* «Мои дела»: блок на главной — заметнее блока дневника, это то, что ждёт ответа. */
.app-tasks { margin-bottom: 14px; background: #008757; color: #fff; border-radius: 14px; padding: 14px 18px; display: flex; flex-direction: column; gap: 2px; text-decoration: none; }
.app-tasks b { font-weight: 700; font-size: 15px; }
.app-tasks span { font-size: 14px; opacity: .92; }
/* Список на странице «Мои дела»; срочные («пора вернуть») — тёплым цветом. */
.task-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: .5rem; }
.task-item { display: flex; flex-direction: column; gap: 2px; padding: 12px 16px; border: 1px solid #e6e4d8; border-radius: 12px; background: #fff; color: var(--ink); text-decoration: none; }
.task-item b { color: var(--green); }
.task-item span { font-size: .9rem; color: #6b6b60; }
.task-item--urgent { border-color: #d9822b; background: #fdf4ea; }
.task-item--urgent b { color: #a4540f; }
/* Число дел рядом с пунктом меню. */
.res-nav-count { display: inline-block; min-width: 1.4em; padding: 0 .4em; border-radius: 999px; background: #008757; color: #fff; font-size: .78rem; line-height: 1.4; text-align: center; }
```

- [ ] **Step 2: Commit**

```bash
cd /c/Projects/skaz-kray-astro && git add residents/public/assets/residents.css && git commit -F - <<'MSG'
«Мои дела»: стили блока, списка и числа в меню

Блок на главной — зелёный, заметнее блока дневника; срочные дела на своей
странице — тёплым цветом.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
MSG
```

---

### Task 13: Полный прогон и выкатка

- [ ] **Step 1: Весь набор тестов**

Run: команда из «Как гонять тесты» **без** `--filter`
Expected: `OK`, все тесты зелёные (было 292, добавится около 30).

- [ ] **Step 2: Синтаксис всех PHP-файлов на сервере**

```bash
cd /c/Projects/skaz-kray-astro/residents && tar -czf - --exclude=./vendor --exclude=./.phpunit.cache --exclude=./public/uploads --exclude=./.git --exclude=./var . 2>/dev/null | ssh abconsult 'tar -xzf - -C /root/ledger-test && cd /root/ledger-test && for f in $(find src public bin -name "*.php"); do php8.3 -l "$f" >/dev/null || echo "СИНТАКС: $f"; done; echo lint-ok'
```

Expected: одна строка `lint-ok`, без `СИНТАКС:`.

- [ ] **Step 3: Спросить у пользователя разрешение на пуш и деплой.** Без явного «да» — не пушить и не выкатывать.

- [ ] **Step 4: Пуш** (после «да»; если `origin` ушёл вперёд — сначала `git pull --rebase`)

```bash
cd /c/Projects/skaz-kray-astro && git push origin main
```

Если падает с «Empty reply from server» — повторить с `git -c http.version=HTTP/1.1 push origin main`.

- [ ] **Step 5: Деплой**

```bash
cd /c/Projects/skaz-kray-astro/residents && bash deploy/deploy.sh --dry-run && bash deploy/deploy.sh
```

Expected: в пробном прогоне «Лишних файлов на сервере нет», затем «Готово».

- [ ] **Step 6: Проверка на живом сервере**

```bash
curl -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://skaz-kray.ru/poselenie/dela
```

Expected: `302 -> https://skaz-kray.ru/poselenie/vhod?next=%2Fposelenie%2Fdela` (без входа страница закрыта и запоминает адрес возврата).

И попросить пользователя открыть приложение: блок «Ждут вас» на главной (если есть дела), пункт «Мои дела» в меню, сама страница.
