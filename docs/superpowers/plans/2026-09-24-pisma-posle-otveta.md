# Письма — после ответа — план

**Спека:** `docs/superpowers/specs/2026-09-24-pisma-posle-otveta-design.md`.
Выполняется сразу в этой сессии, по TDD, тесты — на `abconsult` в
`/root/ledger-test-dela`.

1. Тест в `MailerTest`: `later()` ставит в очередь, `flushLater()` опустошает её
   и не бросает без настроек SMTP. Затем `Mailer::later()`, `flushLater()`,
   `queued()` (для тестов).
2. Переключить на `later()`: помощники `mail()` в четырёх контроллерах,
   восстановление пароля (жители и Совет), приглашение в Совет.
3. Полный прогон, линт, коммит; пуш и деплой — по разрешению.
