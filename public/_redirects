# Только для служебной копии сайта на Netlify (используется исключительно
# как хост для Identity + Git Gateway входа редактора, см.
# docs/superpowers/plans/2026-08-27-editor-login.md). На боевом сайте
# skaz-kray.ru (nginx на VPS) этот файл ничего не делает — Netlify-специфичный
# синтаксис, nginx его просто не читает.
#
# Приглашение по email от Netlify Identity ведёт на корень этого сайта
# (https://<site>.netlify.app/#invite_token=...). 200-рерайт (не редирect!)
# отдаёт содержимое /editor/index.html, не меняя URL и хеш в адресной строке —
# так виджет Identity на /editor/ видит invite_token и сам открывает окно
# "задать пароль", без ручной правки ссылки редактором.
/*  /editor/index.html  200
